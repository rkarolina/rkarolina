git status -> checks branch
git pull -> upload the changes
git branch test -> adds new branch called "test"
git checkout test -> changes branch on "test"
git add test2.txt -> adds a file to the repo
git commit -m "the name of the commit" -> commits changes to the remote repo
git push
git push --set upstream origin test

PR -> in the remote repository
